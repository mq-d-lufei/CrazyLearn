package com.crazy.crazylearn.datastructure.db;

public class DbSummary {

    /***
     *
     * 一、终端操作数据库
     *
     * 1、登录数据库服务器
     * mysql -uroot -p123456
     *
     * 2、查询数据库服务器中所有数据库
     * show databases
     *
     * 3、选中某一个数据库
     * use somedatabase
     *
     * 4、查询数据库表中所有数据
     * select * from admin
     * select * from admin where id = 1
     *
     * 5、退出数据库服务器
     * exit
     *
     * 6、数据库服务器中创建数据库
     * create database test
     *
     * 7、查看所有表信息
     * show tables
     *
     * 8、创建一个数据表
     * create table pet(
     * 	name varchar(20),
     * 	owner varchar(20),
     * 	sex char(1),
     * 	birth date);
     *
     *
     *
     */
}
