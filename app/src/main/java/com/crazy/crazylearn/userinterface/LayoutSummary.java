package com.crazy.crazylearn.userinterface;

public class LayoutSummary {

    /**
     *
     *  测量尺寸(测量宽和测量高、想要在父类中具有的大小)
     *      getMeasuredWidth()
     *      getMeasuredHeight()
     *  实际尺寸（绘制时和布局后的实际大小）
     *      getWidth()
     *      getHeight()
     *
     */
}
